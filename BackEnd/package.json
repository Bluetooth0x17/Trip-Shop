{
  "name": "trip-shop-backend",
  "version": "1.0.0",
  "description": "API pour l'application Trip Shop",
  "main": "server.js",
  "author": "Bluetooth0x17",
  "license": "ISC",

  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.6.3",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2"
    // Ajoutez ici toutes les autres dépendances nécessaires (ex: helmet, morgan, etc.)
  },

  "devDependencies": {
    "nodemon": "^3.0.1"
    // Ajoutez ici d'autres outils de développement (ex: jest, supertest, etc.)
  }
}
MONGODB_URI=mongodb+srv://MadBadByte:<db_password>@cluster0.optdryx.mongodb.net/?appName=Cluster0
